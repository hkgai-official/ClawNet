// agents.events.ts
// Event broadcasting is centralized in AgentEventBus. This file exists for
// symmetry with other feature folders and to host any future agent-specific
// emit helpers (e.g. for service-driven local events that don't originate from gateway push).
export {};
